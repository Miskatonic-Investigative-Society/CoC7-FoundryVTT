<section class="tab content flexcol scrollable {{tab.cssClass}}" data-group="primary" data-tab="content">
  {{#if tabs.content}}
    <fieldset class="flexcol" style="flex: 1">
      <prose-mirror name="system.content" value="{{document.system.content}}" data-document-uuid="{{document.uuid}}" toggled>{{{enrichedContent}}}</prose-mirror>
    </fieldset>
  {{/if}}
</section>